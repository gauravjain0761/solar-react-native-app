import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const MaintenenceForm = (props: Props) => {
  return (
    <View>
      <Text>MaintenenceForm</Text>
    </View>
  );
};

export default MaintenenceForm;

const styles = StyleSheet.create({});
