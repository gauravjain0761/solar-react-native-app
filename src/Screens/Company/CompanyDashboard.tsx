import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

type Props = {};

const CompanyDashboard = (props: Props) => {
  return (
    <View>
      <Text>CompanyDashboard</Text>
    </View>
  );
};

export default CompanyDashboard;

const styles = StyleSheet.create({});
